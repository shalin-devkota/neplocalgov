 matches_province = not province or entry["province"] == province
    #     matches_district = not district or entry["district"] == district

    #     if matches_province and matches_district:
    #         filtered_data.append(entry)

    # if not filtered_data:
    #     raise ValueError("No matching entries found with the given criteria.")

    # return json.dumps(filtered_data, indent=4)